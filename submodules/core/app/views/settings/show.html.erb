<% content_for :title, "Settings" %>
<div class="py-10">
  <div class="">
    <h1>Workspace Settings</h1>
    <p class="text-base opacity-80 mt-2">Manage your workspace settings and preferences.</p>
  </div>

  <div class="mt-10 flex flex-col gap-y-12">

    <div>
      <div class="flex items-center gap-x-2 text-xl">
        <i class="iconoir-globe text-primary"></i>
        <h2 class="">Workspace</h2>
      </div>
      <div class="w-ful h-px bg-base-300 mt-2 mb-4"></div>
      <div class="grid grid-cols-3 gap-x-4 gap-y-8">
        <%= render "shared/setting_card",
                   path: edit_settings_general_path,
                   title: 'General',
                   description: 'Configure basic website settings, language, and branding options',
                   button_title: 'Configure settings',
                   icon: 'iconoir-settings'
        %>
        <%= render "shared/setting_card",
                   path: settings_api_tokens_path,
                   title: 'API Tokens',
                   description: 'Manage API tokens for external integrations.',
                   button_title: 'Manage tokens',
                   icon: 'iconoir-lock-square'
        %>
      </div>
    </div>

  </div>
</div>
