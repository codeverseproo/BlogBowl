<header class="border-b fixed top-0 left-0 right-0 z-20 bg-base-100">
  <div class="navbar max-w-7xl mx-auto px-5 md:px-0 py-1">
    <div class="navbar-start">
      <a class="text-2xl" href='<%= @page_settings.logo_link&.presence || dynamic_prefix('/') %>'>
        <% if @page_settings.logo.attached? %>
          <%= image_tag @page_settings.logo, class: "h-10 object-contain", alt: @page_settings.logo_text %>
        <% else %>
          <%= @page_settings.logo_text %>
        <% end %>
      </a>
    </div>
    <div class="navbar-center hidden lg:flex">
      <ul class="menu menu-horizontal px-1 gap-x-1">
        <% @navbar_links.each do |link| %>
          <li><a class="font-medium text-base opacity-70" href="<%= link.url %>"><%= link.title %></a></li>
        <% end %>
      </ul>
    </div>
    <% header_cta_url = @page_settings.header_cta_button_link.presence %>
    <% show_header_cta = @page_settings.header_cta_enabled? && header_cta_url.present? %>
    <div class="navbar-end gap-x-2">
      <%= render "layouts/public/shared/search_modal" %>
      <div class="dropdown dropdown-end">
        <div tabindex="0" role="button" class="btn btn-square md:hidden">
          <i class="iconoir-menu text-xl"></i>
        </div>
        <ul
          tabindex="0"
          class="menu menu-sm dropdown-content bg-base-100 rounded-box z-[1] mt-3 w-52 p-2 shadow gap-y-2">
          <% @navbar_links.each do |link| %>
            <li><a class="font-medium text-base opacity-70" href="<%= link.url %>"><%= link.title %></a></li>
          <% end %>
          <% if show_header_cta %>
            <li>
              <a class="btn btn-primary" target="_blank" href="<%= header_cta_url %>"><%=  @page_settings.header_cta_button.present? ? @page_settings.header_cta_button : 'CTA button' %></a>
            </li>
          <% end %>
        </ul>
      </div>
      <% if show_header_cta %>
        <a class="btn btn-primary hidden md:flex" target="_blank" href="<%= header_cta_url %>"><%=  @page_settings.header_cta_button.present? ? @page_settings.header_cta_button : 'CTA button' %></a>
      <% end %>
    </div>
  </div>

  <%= render "layouts/public/shared/progress-bar" %>
</header>
