<%= link_to dynamic_prefix(public_category_path(category)), class: "flex flex-col items-center justify-center border rounded p-2" do %>
  <h2 class="mt-2 font-bold"><%= category.name %></h2>
  <p class="mt-2 text-gray-500"><%= category.description %></p>
<% end %>
